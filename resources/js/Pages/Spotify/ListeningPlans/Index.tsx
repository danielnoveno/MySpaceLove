import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";
import { Head, Link } from "@inertiajs/react";
import { Plus } from "lucide-react";
type Space = { slug: string; title: string };
type Plan = { id: number; title: string; description: string | null; scheduled_at: string | null; spotify_playlist_id: string | null; user: string | null };
export default function ListeningPlansIndex({ space, plans }: { space: Space; plans: Plan[] }) { return <AuthenticatedLayout><Head title="Listening Plans" /><div className="w-full max-w-none px-0 sm:px-6 lg:px-8"><Header space={space} />{plans.length === 0 ? <div className="rounded-3xl border border-dashed border-green-200 bg-green-50/70 p-10 text-center text-gray-600">No listening plans yet.</div> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{plans.map((p) => <article key={p.id} className="rounded-3xl border border-green-100 bg-white/90 p-5 shadow-sm"><h2 className="text-lg font-semibold text-gray-900">{p.title}</h2>{p.description && <p className="mt-2 text-sm text-gray-600">{p.description}</p>}<p className="mt-3 text-sm text-green-600">{p.scheduled_at ? new Date(p.scheduled_at).toLocaleString() : "No schedule"}</p>{p.spotify_playlist_id && <p className="mt-2 text-xs text-gray-400">Playlist: {p.spotify_playlist_id}</p>}<p className="mt-2 text-xs text-gray-400">By {p.user ?? "Member"}</p></article>)}</div>}</div></AuthenticatedLayout>; }
function Header({ space }: { space: Space }) { return <div className="mb-6 flex flex-wrap items-center justify-between gap-4"><div><Link href={route("spotify.index", { space: space.slug })} className="text-sm font-semibold text-pink-500 hover:text-pink-600">← Spotify</Link><h1 className="mt-2 text-3xl font-semibold text-gray-900">Listening Plans</h1></div><Link href={route("spotify.listening-plans.create", { space: space.slug })} className="inline-flex items-center gap-2 rounded-full bg-green-500 px-4 py-2 text-sm font-semibold text-white hover:bg-green-600"><Plus className="h-4 w-4" /> Create</Link></div>; }
