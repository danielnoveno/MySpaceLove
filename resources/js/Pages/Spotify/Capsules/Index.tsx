import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";
import { Head, Link } from "@inertiajs/react";
import { Plus } from "lucide-react";
type Space = { slug: string; title: string };
type Capsule = { id: number; track_name: string; artists: string; moment: string | null; description: string | null; saved_at: string | null; preview_url: string | null; user: string | null };
export default function CapsulesIndex({ space, capsules }: { space: Space; capsules: Capsule[] }) { return <AuthenticatedLayout><Head title="Spotify Capsules" /><div className="w-full max-w-none px-0 sm:px-6 lg:px-8"><Header space={space} title="Memory Capsules" createRoute={route("spotify.capsules.create", { space: space.slug })} />{capsules.length === 0 ? <Empty text="No capsules yet." /> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{capsules.map((c) => <article key={c.id} className="rounded-3xl border border-amber-100 bg-white/90 p-5 shadow-sm"><h2 className="text-lg font-semibold text-gray-900">{c.track_name}</h2><p className="text-sm text-amber-600">{c.artists}</p>{c.moment && <p className="mt-3 rounded-2xl bg-amber-50 p-3 text-sm text-amber-800">{c.moment}</p>}{c.description && <p className="mt-3 text-sm text-gray-600">{c.description}</p>}{c.preview_url && <audio className="mt-3 w-full" controls src={c.preview_url} />}<p className="mt-3 text-xs text-gray-400">{c.saved_at ?? "No date"} • {c.user ?? "Member"}</p></article>)}</div>}</div></AuthenticatedLayout>; }
function Header({ space, title, createRoute }: { space: Space; title: string; createRoute: string }) { return <div className="mb-6 flex flex-wrap items-center justify-between gap-4"><div><Link href={route("spotify.index", { space: space.slug })} className="text-sm font-semibold text-pink-500 hover:text-pink-600">← Spotify</Link><h1 className="mt-2 text-3xl font-semibold text-gray-900">{title}</h1></div><Link href={createRoute} className="inline-flex items-center gap-2 rounded-full bg-green-500 px-4 py-2 text-sm font-semibold text-white hover:bg-green-600"><Plus className="h-4 w-4" /> Create</Link></div>; }
function Empty({ text }: { text: string }) { return <div className="rounded-3xl border border-dashed border-green-200 bg-green-50/70 p-10 text-center text-gray-600">{text}</div>; }
