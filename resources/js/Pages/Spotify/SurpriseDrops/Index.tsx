import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";
import { Head, Link } from "@inertiajs/react";
import { Plus } from "lucide-react";
type Space = { slug: string; title: string };
type Drop = { id: number; track_name: string; artists: string; scheduled_for: string | null; note: string | null; curator_name: string | null; user: string | null };
export default function SurpriseDropsIndex({ space, drops }: { space: Space; drops: Drop[] }) { return <AuthenticatedLayout><Head title="Surprise Drops" /><div className="w-full max-w-none px-0 sm:px-6 lg:px-8"><div className="mb-6 flex flex-wrap items-center justify-between gap-4"><div><Link href={route("spotify.index", { space: space.slug })} className="text-sm font-semibold text-pink-500 hover:text-pink-600">← Spotify</Link><h1 className="mt-2 text-3xl font-semibold text-gray-900">Surprise Drops</h1></div><Link href={route("spotify.surprise-drops.create", { space: space.slug })} className="inline-flex items-center gap-2 rounded-full bg-green-500 px-4 py-2 text-sm font-semibold text-white hover:bg-green-600"><Plus className="h-4 w-4" /> Create</Link></div>{drops.length === 0 ? <div className="rounded-3xl border border-dashed border-green-200 bg-green-50/70 p-10 text-center text-gray-600">No surprise drops yet.</div> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{drops.map((d) => <article key={d.id} className="rounded-3xl border border-pink-100 bg-white/90 p-5 shadow-sm"><h2 className="text-lg font-semibold text-gray-900">{d.track_name}</h2><p className="text-sm text-pink-600">{d.artists}</p><p className="mt-3 text-sm text-green-600">{d.scheduled_for ? new Date(d.scheduled_for).toLocaleString() : "No schedule"}</p>{d.note && <p className="mt-3 rounded-2xl bg-pink-50 p-3 text-sm text-pink-800">{d.note}</p>}<p className="mt-2 text-xs text-gray-400">By {d.curator_name ?? d.user ?? "Member"}</p></article>)}</div>}</div></AuthenticatedLayout>; }
